import { StyleSheet } from "react-native";

export const LoginStyles = StyleSheet.create({
  componentContainer: {
    padding: 20,
    justifyContent: "center",
    width: "100%",
    gap: 20,
  },

  input: {
    marginBottom: 12,
  },
});
